# Spring AI

## Using OpenAI

## Using Ollama locally

- install [Ollama](https://ollama.com/downloads)
- run `ollama run llama3.2` to download the model
- add dependencies to your `build.gradle` file ``

- `./gradlew bootRun --quiet | jq -C .`
